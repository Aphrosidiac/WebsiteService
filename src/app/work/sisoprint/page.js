import SisoPrintClient from './SisoPrintClient';

export const metadata = {
  title: 'SisoPrint — AP Devotion',
  description: 'A full-featured online print service platform built with React 19, TypeScript, and Vite.',
};

export default function SisoPrintPage() {
  return <SisoPrintClient />;
}
